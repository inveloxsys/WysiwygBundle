fos_js_routing:
    resource: "@FOSJsRoutingBundle/Resources/config/routing/routing.xml"

_wysiwyg:
    path:     /_wysiwyg/{selector}/{flavor_key}.{_format}
    defaults: { _controller: MuchoMasFacilWysiwygBundle:Default:index, _format: js }
    options:
        expose: true
