if (typeof tinymce == 'undefined') {
    alert('You must load the CKEDITOR .js file in your page. http://www.tinymce.com/');
}
else {
    tinymce.init({
        selector: "{{ selector|raw }}"
    });
}